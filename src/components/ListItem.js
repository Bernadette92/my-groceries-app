import React from "react";

function ListItem(title) {
  console.log(title);
  return (
    <div className="list">
      {/* <p>ID: {title.id}</p>
      <p>title: {title.title}</p> */}
    </div>
  );
}

export default ListItem;
